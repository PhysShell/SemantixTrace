# 07 — External validation (RCAEval)

Status: **not unlocked — intentionally.**

The preregistration locked an RCAEval subset as confirmatory data for "a
promising method" frozen after development. No promising method exists:
S1 and S2 met the frozen kill criteria on development data (H1, H2
falsified; `04-…`/`05-…`). Downloading and evaluating the confirmatory
set on a falsified method would burn the lock for nothing — the lock is
this experiment's only unconsumed confirmatory resource, and it remains
intact for any future, redesigned method (e.g. one with a
causally-grouped representation).

Accordingly: **no RCAEval data was ever downloaded into this workspace**;
every claim in this experiment is explicitly development/exploratory
(D-001), and no confirmatory claim is made anywhere.

Recorded as D-007 in `decisions.log.md`.
